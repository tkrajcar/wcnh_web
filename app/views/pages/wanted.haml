.column.grid_12
  %h1 Wanted List
  .body
    - @wanted.each do |i|
      %h4= "Wanted Entry #{i._id}"
      %p
        %strong Name: 
        = "#{i.name}"
        %br
        %strong Bounty: 
        = "#{number_with_delimiter(i.amount, :delimiter => ",")}"
        %br
        %strong Contact:
        = "#{i.contact}"
        %br
        %strong Details: 
        = "#{i.info}"
