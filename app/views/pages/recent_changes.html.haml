-title_and_h1 "Recent Changes"

.column.grid_12.body
  %table
    %tr
      %th Title
      %th Author
      %th Date
      %th Changes
    -@pages.each do |page|
      %tr
        %td= link_to page.title, "pages/#{page.urls.first}"
        %td= page.author
        %td= page.updated_at.strftime("%d %b %Y %H:%M %Z")
        -if page.versions.count > 0
          -change = page.versions.last.text.length - page.versions[page.versions.count - 2].text.length
          -if change > 0
            %td= "#{change.abs} deletions"
          -else
            %td= "#{change} additions"
        -else
          %td= "New Page"